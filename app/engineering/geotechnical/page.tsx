"use client";

import { SettlementDisplacementChart } from "@/components/engineering/SettlementDisplacementChart";

export default function GeotechnicalPage() {
  const exportCertificate = () => { const printWindow = window.open("", "_blank"); if (!printWindow) return; printWindow.document.write("<html><body style='font-family:Arial;padding:40px'><h1>Quadillar LiveView</h1><h2>Geotechnical Safety Compliance Certificate</h2><p>Foundation settlement, retaining wall, piezometer and IS 2911 pile load telemetry reviewed by Structural Consultant.</p><h3>Finding: ZERO ACTION THRESHOLD BREACHES</h3><p>Issued: " + new Date().toLocaleDateString("en-IN") + "</p></body></html>"); printWindow.document.close(); printWindow.print(); };
  return <main style={page}><div style={shell}><header style={header}><div><div style={eyebrow}>Engineering / geotechnical controls</div><h1 style={heading}>Deep foundation settlement telemetry</h1><p style={muted}>Instrumented view of retaining wall movement, optical prisms, groundwater response, and pile capacity.</p></div><button type="button" onClick={exportCertificate} style={button}>Export Geotechnical Safety Compliance Certificate</button></header><section style={status}><strong>FOUNDATION HEALTH: STABLE</strong><span>Zero action breaches · 12 sensors online · last sync 18:00</span></section><SettlementDisplacementChart /></div></main>;
}
const page = { minHeight: "100vh", background: "#050816", color: "#e2e8f0", padding: 28 }; const shell = { maxWidth: 1250, margin: "0 auto", display: "grid", gap: 20 }; const header = { display: "flex", justifyContent: "space-between", alignItems: "flex-start", gap: 18, flexWrap: "wrap" as const }; const eyebrow = { color: "#7dd3fc", fontSize: 11, letterSpacing: ".16em", textTransform: "uppercase" as const }; const heading = { margin: "7px 0 0", fontSize: 40, letterSpacing: "-.04em" }; const muted = { color: "#94a3b8", fontSize: 13, lineHeight: 1.5 }; const button = { border: 0, borderRadius: 9, background: "#22c55e", color: "#052e16", padding: "11px 14px", fontWeight: 800, cursor: "pointer" }; const status = { display: "flex", justifyContent: "space-between", gap: 12, flexWrap: "wrap" as const, background: "rgba(34,197,94,.1)", border: "1px solid rgba(34,197,94,.35)", borderRadius: 12, padding: 15, color: "#86efac" };