import { fetchDashboardSnapshot, fetchProjectTasks } from "@/app/lib/services";
import { Bim4DTimelinePlayer } from "@/components/viewer/Bim4DTimelinePlayer";

export default async function FourDSimulatorPage() { const [tasks, snapshot] = await Promise.all([fetchProjectTasks("proj-1"), fetchDashboardSnapshot("proj-1")]); return <main style={page}><div style={shell}><header><div style={eyebrow}>Engineering / digital twin controls</div><h1 style={heading}>4D BIM construction simulator</h1><p style={muted}>Replay planned sequencing against actual as-built progress and expose schedule variance before it reaches the critical path.</p></header><Bim4DTimelinePlayer tasks={tasks} clashes={snapshot.bimClashes ?? []} /></div></main>; }
const page = { minHeight: "100vh", background: "#050816", color: "#e2e8f0", padding: 28 }; const shell = { maxWidth: 1420, margin: "0 auto", display: "grid", gap: 20 }; const eyebrow = { color: "#7dd3fc", fontSize: 11, letterSpacing: ".16em", textTransform: "uppercase" as const }; const heading = { margin: "7px 0 0", fontSize: 40, letterSpacing: "-.04em" }; const muted = { color: "#94a3b8", fontSize: 13, lineHeight: 1.5 };